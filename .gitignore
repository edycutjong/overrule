# ── Dependencies ─────────────────────────────────────────────
node_modules/

# ── Generated / deterministic artifacts (re-created by scripts) ─
out/
fixtures/generated/
*.tsbuildinfo
coverage/
.vitest/

# ── Secrets & environment (NEVER commit; live keys are online-path) ─
.env
.env.*
!.env.example

# ── Logs & OS cruft ──────────────────────────────────────────
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# graphify knowledge graph (kitchen — never judge-facing)
graphify-out/

# ── Vercel CLI local link (would override VERCEL_PROJECT_ID in CI) ──
.vercel/
verify/.vercel/
